# ai-speech

Telegram bot middleware for meeting analysis: downloads audio/video, transcribes with Groq STT, analyzes with Groq LLM, and posts a card to Planka.

## Quick start
1. Copy `.env.example` to `.env` and fill in values.
2. Install dependencies: `npm install`
3. Run: `npm start`
